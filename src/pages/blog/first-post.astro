---
import Layout from '../../layouts/Layout.astro';
import RandomJoke from '../../components/RandomJoke.astro';
import ThemePreview from '../../components/ThemePreview.astro';
const date = new Date('2025-05-10').toLocaleDateString();
---

<Layout>
  <main style="max-width: 700px; margin: 2rem auto; padding: 2rem; font-family: inherit; background: rgba(7,54,66,0.07); border-radius: 1.5rem; box-shadow: 0 4px 32px rgba(0,43,54,0.07);">
    <h1 style="font-size: 2rem; color: var(--accent);">First Post (.astro Example)</h1>
    <p style="color: var(--fg); font-size: 1.1rem;">This post demonstrates the power of <code>.astro</code> files for blog posts. You can use components, dynamic data, and custom HTML!</p>
    <p style="color: var(--base01); font-size: 0.95rem;">Published: <strong>{date}</strong></p>
    <blockquote style="border-left: 4px solid var(--accent); margin: 1.5rem 0; padding: 1rem; background: var(--base2); color: var(--base01); font-style: italic;">
      Astro lets you mix Markdown simplicity with the full power of components and JavaScript.
    </blockquote>
    <ul style="margin-bottom: 1.5rem;">
      <li>Use <strong>Astro components</strong> for custom layouts</li>
      <li>Embed <span style="color: var(--green);">dynamic data</span> and logic</li>
      <li>Mix in <span style="color: var(--blue);">HTML, SVG, or even React/Vue/Svelte</span></li>
    </ul>
    <ThemePreview />
    <RandomJoke />
    <div style="margin: 2rem 0;">
      <pre style="background: var(--base02); color: var(--base1); padding: 1rem; border-radius: 0.5rem; overflow-x: auto;"><code>{`// Example Astro component usage
import MyComponent from '../components/MyComponent.astro';

<MyComponent message="Hello from Astro!" />`}</code></pre>
    </div>
    <div style="margin: 2rem 0; text-align: center;">
      <svg width="120" height="80" viewBox="0 0 120 80" fill="none" xmlns="http://www.w3.org/2000/svg">
        <circle cx="60" cy="20" r="15" stroke="#b58900" stroke-width="3" fill="#fdf6e3"/>
        <line x1="60" y1="35" x2="60" y2="65" stroke="#b58900" stroke-width="3"/>
        <line x1="60" y1="45" x2="40" y2="55" stroke="#b58900" stroke-width="3"/>
        <line x1="60" y1="45" x2="80" y2="55" stroke="#b58900" stroke-width="3"/>
        <line x1="60" y1="65" x2="50" y2="75" stroke="#b58900" stroke-width="3"/>
        <line x1="60" y1="65" x2="70" y2="75" stroke="#b58900" stroke-width="3"/>
        <text x="60" y="18" font-size="10" text-anchor="middle" fill="#cb4b16">:D</text>
      </svg>
      <div style="color: var(--yellow); font-size: 1.1rem; margin-top: 0.5rem;">Custom SVG artwork in .astro!</div>
    </div>
    <a href="/blog/" style="color: var(--accent);">← Back to Blog</a>
  </main>
</Layout>
