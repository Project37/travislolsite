import { expect, afterEach } from 'vitest';
import '@testing-library/jest-dom';

// Clean up after each test
afterEach(() => {
  // We'll use just afterEach hook without cleanup since we're not testing DOM directly
});
